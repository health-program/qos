<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.paladin.qos.mapper.shejike.AverageNumberMapper">

	<select id="getTotalNum" parameterType="Map" resultType="Long">
		select round(count(*)/count(distinct(DOCTCODE)),2) as averageNumber from phis_webhis.V_OPM_RECIPE_INFO  a
              where PRESSTATUS = '1' or PRESSTATUS = '2'
		<where> 
		     1=1
			<!-- <if test="startTime != null and startTime !='' ">
				and a.REGDATE <![CDATA[>=]]>
				#{startTime}
			</if>
			<if test="endTime != null and endTime !='' ">
				and a.REGDATE <![CDATA[<]]>
				#{endTime}
			</if>
			<if test="unitId != null and unitId !='' ">
				and a.ORGCODE = #{unitId}
			</if> -->
		</where>
	</select>


</mapper>
